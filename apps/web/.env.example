# Public URL of this web app (used for links / redirects).
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Base URL of the ABLE backend API (Express, base path /api).
NEXT_PUBLIC_API_URL=http://localhost:4000
